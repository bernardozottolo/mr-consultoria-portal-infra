# Variáveis de ambiente para portal-infra
# Copie este arquivo para .env e ajuste os valores

# JWT Secret - OBRIGATÓRIO em produção
# Gere um secret seguro: openssl rand -hex 32
JWT_SECRET=change-me-in-production-use-openssl-rand-hex-32

# Google Service Account
# O arquivo JSON deve estar em ./secrets/google.json
# A variável GOOGLE_SERVICE_ACCOUNT_FILE é definida no docker-compose.yml

# Opcional: Configurações adicionais do backend
# FLASK_ENV=production
# LOG_LEVEL=INFO
# ENABLE_CORS=false
